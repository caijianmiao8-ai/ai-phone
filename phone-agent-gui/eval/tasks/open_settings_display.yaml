name: open_settings_display
app: com.android.settings
steps:
  - name: open_display
    action_hint:
      action: tap
      target: {strategy: uiauto, query: "contains=Display OR contains=显示"}
    postcheck:
      - "ui_contains:text=Display"
